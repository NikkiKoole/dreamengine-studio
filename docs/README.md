# docs/ — map & organizing rule

These docs are organized by **genre and durability**, not by topic — because the mess
they replaced came from one file trying to be reference *and* status *and* rationale at
once, so part of it was always stale. Each file now answers one kind of question.

> **The rule.** **Status** lives only in `STATUS.md`. **Settled rationale** lives only in
> `decisions/`. **Vision/philosophy** lives only in `VISION.md`. **Exploratory design**
> lives in `design/`. Docs *cross-reference*; they don't *duplicate*. If two docs say the
> same thing, one is wrong — fix it at the source and link.
>
> **Cross-reference a section with a numbered `§`-ref** (`audio-notes §2`), **not a
> `#anchor`.** `§`-refs are the house convention and `lint-docs.js` validates them
> (it deliberately ignores `#anchors`); a repo scan found only 11 anchor links in ~1900 —
> the corpus doesn't use them. The one exception is `HANDOFF.md`, whose Resume-at pointers
> ARE anchor links on purpose, validated by `handoff.js --check` (see
> [`design/driftable-docs.md`](design/driftable-docs.md) — the two-door pattern). Settled;
> don't re-litigate.

## Layout

```
docs/
├── README.md          you are here — the map
├── VISION.md          why & what: product idea, audience, philosophy, console spec
├── what-is-this.md    the manifesto / origin story (moved from field-notes 2026-06-30) — what DreamEngine is & is becoming
├── onepiecofadvice.md a short note on mindset — companion to what-is-this.md
├── STATUS.md          state: the one ledger of shipped / open / cut
├── POLISH_TODO.md     work list: the per-game "juice pass" across the carts (dormant idea-bank)
├── cart-polish-punchlist.md  MIGRATED pointer: per-cart polish items now live in each cart's de:meta.todo[] (query: node tools/cart-todos.js); this file keeps the maker's framing notes. Systemic items → design/action-plan.md
├── HANDOFF.md         narrative + environment gotchas (not obvious from code/git)
├── decisions/         frozen, dated decisions (ADR-lite) — the "why we (didn't) do X"
├── design/            exploratory design notes (scratchpads) — rationale + proposals
│   ├── design-system.md  ★ LIVING REFERENCE: the single VISUAL CONTRACT — 320×200 native, the pico32 palette (32 colours, index+CLR_+hex), the FONT_* type family, the keyboard/gamepad/mouse/touch input model, and the house UI conventions (short verbs, British greys, set-and-hold effects, the two-part bar). A mirror of studio.h/pico32.json/studioDocs.js — hand it to a designer (human or Claude). Has a click-to-copy Artifact companion
│   ├── api-notes.md     engine API: classics survey, signatures, naming, cart-patterns
│   ├── engine-simplification.md  READY-TO-BUILD cleanup backlog: duplication/missing-helpers/naming in studio.c + sound.h + the library headers (clamp01/voice_white/dc_block, sw/de_cpu primitive twins, choke-group + FX-setter dupes, table-driven dispatch). Behaviour-preserving refactors, each with its verifying gate
│   ├── cpu-shaders.md   CPU "fragment shaders" (pset_rgb/pget_rgb): the shipped teaching trilogy (shadelab → caustics → raymarch) + shadermath.h, and the parked ideas (#3 make per-pixel cost visible, #5 a true offscreen buffer for multipass)
│   ├── cart-as-script.md  EXPLORATION: run cart C without an external compiler via libtcc (in-memory compile + hot-reload); the 3-symbol host↔cart boundary, why Wasmer clang/WASIX won't do graphics, native goal vs. browser goal
│   ├── external-data-carts.md  EXPERIMENT: a cart loads a data blob at RUNTIME (--data <file> → de_data_path() + runtime/json.h) instead of baking it into source — kills the floorwalker/seinelaan bake-a-cart-per-dataset smell. The shared "vector features" JSON schema, the OSM/Overpass fetcher (data-tools/roadview/osm-roads.js, with --demo), the roadview consumer cart, and where to get real road data. Not committed API
│   ├── cart-os.md  EXPLORATION (fermenting): carts as TOOLS in a shared environment — an OS/shell/Amiga-Workbench where you pipe one cart into another. The key finding: the kernel already exists (main.cjs spawns/manages cart processes) and a one-stage pipe already ships (osm-roads | roadview via --data). The fork (Node-side orchestration = cheap & powerful vs C/in-engine = on-brand but fights one-window/one-process Raylib), three tiers (pipes → shell → Workbench), the Tier-1 MVP (build/fs/ shared dir + --out flag), and the honest north-star/DIV-ghost(ADR-0001) tensions
│   ├── audio-notes.md   sound HUB: current surface map (§2), parameter placement (§4), §12 instrument gaps, §13 SCWs-vs-engines, §14 Roland machine readiness — tops with the audio-family index ("where does an audio idea go?")
│   ├── instrument-engines.md  the navkit rich-instrument port program (was audio-notes §8 — numbering preserved): 3-macro discipline §8.1.1, phase ordering §8.5, shipping playbook §8.8.2, §8.9 candidate engine catalog, §8.10 effects layer (+ §8.10.1 PARKED interim items)
│   ├── piano-engine.md  INSTR_PIANO/StifKarp diagnosis: why it reads as a HARP (faithful port of a pluck-in-piano-costume) + the fix roadmap (double decay, hammer knock, velocity coupling, stretched tuning — the Feynman ref)
│   ├── audio-timing.md   EXPLORATION: why web playback feels "drunk"/drifts — the beat clock advances by per-frame dt (frame-quantized triggers), web's dt is jittery AND was unclamped so hitches lurch the sequence & drop beats; the clamp fix + the schedule-ahead (radio.h) vs frame-triggered (every()) split + the sample-clock cure
│   ├── audio-threading.md  SPEC: the game-thread/audio-thread model — the lock-free SPSC queue (already in sound.h), who runs the audio thread per platform (native/web-ScriptProcessor/web-worklet/iOS/Switch), the atomics-vs-volatile correctness tax, web's SharedArrayBuffer/COOP-COEP wrinkle, the runtime backend pick (worklet when isolated else ScriptProcessor), and the staged build plan
│   ├── stereo.md         SPEC (design call ahead of build): resolves §9 "Stereo?" — instrument_pan/note_pan + LFO_PAN, linear pan law (center byte-identical), echo mono in v1; the §8.10 effects-layer prerequisite + a pre-flight bite checklist
│   ├── spatial.md        positional audio (OpenAL-style listener + sources): v1 per-voice (listener/note_pos/note_motion/hit_at) + v2 emitter buses (instrument_pos/_motion — a whole effected source moves) both SHIPPED; v3 acoustic zones (inside/outside/occlusion/materials) PROPOSED. Showcase cart = spatial
│   ├── future-stations.md  the radio-station candidates parking lot (was game-music's "Future stations"): ranked candidates, the citypop conditions, new-brains-per-cart axis, IDM + functional-music wings, the "newest demand" scoring
│   ├── radio-genre-fidelity.md  gap ledger: each station's genre-true IDEAL (researched blind) vs what we built — the holes (faked/missing instruments where a real engine sits unused, the generative brains we lack), aggregated + per-station
│   ├── sound-next-steps.md  the actionable digest of the 2026-06-10 sound research: prioritized upgrade-wirings, new instruments/stations to build, new generative brains, and decisions pending — each linking to the doc with detail
│   ├── unison-primitive.md  SHIPPED (2026-07-09, ADR-0030): the hand-rolled multi-slot detune trick promoted to instrument_unison(slot, voices, detune) + a live instrument_unison_detune twin + LFO_/ENV_DETUNE dests — the JP-8000 Super Saw bloom, motion-recordable; corrected trigger (moog/mt70 are NOT unison). Showcase cart = supersaw
│   ├── effects-bus-architecture.md  detail map for 3 parked routing increments: reorderable per-bus inserts (A), multi-reverb sparse tank pool (B), reverb-as-bus-insert (C) — sketches + cost ledger; "the engine is already a mixing console"
│   ├── radio-instrument-options.md  per-station timbre swaps / engine retrofits, ranked per engine (mallet → lowend's vibraphone, etc.)
│   ├── held-notes.md    SHIPPED spec: note_on/note_off handles + live note_pitch/vol/cutoff/duty + slew — the sustain layer the instruments ride on
│   ├── flight-recorder.md  BUILDING/v1 SHIPPED (2026-07-10): always-on deterministic session capture — every ▶ Run records to a rolling scratch ring (build/.rec/, thrown away automatically); "keep take" (⌘⇧K) promotes to tools/clips for a clip or an exact rr-style bug repro. Enabled by a `# seed` header the recorder writes + load_replay reads back. The harness recorder, distinct from the music looper below
│   ├── input-recording-looper.md  exploration: can a cart record the player and loop it back — recording levels for music looping + gameplay ghosts
│   ├── portapop.md  BUILDING (2026-07-10): `portapop` — the Tascam cassette 4-track reimagined as a cart (loopstation grown up into linear tape + a mixing console; showcases the per-slot mixer family). Records control events not PCM (honest boundary named). Design fully specced; SHIPPED phases 1–3 (`tools/carts/portapop.c`): record→loop seam, full upright pizz surface + TUNE toggle, multitrack + CONSOLE mixer (level/pan/VU/arm/mute) + overdub monitoring + TAKE↔CONSOLE mode-flip, and three castable voices (upright BASS · DRUM kit · stage RHODES). LEFT: ping-pong bounce + the tape glue + cassette chrome (phase 4) and the remaining 5 of 8 shelf voices — live punch-list in the cart's `de:meta.todo[]`
│   ├── mic-and-sampling.md  EXPLORING (2026-07-05, not scheduled): what "sampling, fantasy-console-like" could mean on mic-equipped iOS devices — four tiers (mic-as-controller / mic→single-cycle oscillator / resynthesis-not-playback / true PCM at SK-1 specs), the capture-then-freeze determinism rule, and the touchstones (SK-1, 12-bit SP-1200, Roland SP-404, PO-33); the sampler doctrine's written "unless" clause
│   ├── share-panel.md  EXPLORING (2026-07-03): one editor "share" surface for every sharing action (no Xcode ever; every action = a CLI tool first, button streams it) + the NEW UNIT: an app MANIFEST (`apps/<name>/app.json`) so several carts ship as ONE app (Tinyjam) — the multi-cart engine seam scoped (entry-point renaming, per-slug assets, de_state slot, launcher-as-cart + de_switch_cart), three rungs (regroup+clip button / manifest+Mac rehearsal / TestFlight). Seam-#1 SPIKE PASSED same day: acidrack+yachtrack in one binary, zero cart edits (tools/bundle-spike/)
│   ├── sharing-channels.md  ★ STOCK-TAKE (2026-07-03): the MAP of every way a cart reaches someone — three channels (A web showcase on an own domain · B the App Store · C friends-and-family files) with per-channel ships-today / built-not-wired / missing ladders, the editor-as-share-panel backlog table (gif-from-recipe, itch mirror, batch publish), and the three maker-only open decisions. The map, not the owner — links out to ADR-0020/0023, ios-plan, sharing.md, exporting.md
│   ├── demand-generation.md  ★ STRATEGY (2026-07-03): the honest hierarchy of what drives downloads + paid conversions — capture (ASO/keywords, the tail) vs generation (video/tribe/word-of-mouth, the wave); you grab a TRIBE not "the world". Orders the tactical docs by leverage (delightful+clippable product > shareable video > gift-first tribe outreach > free-web funnel > store-page conversion > keywords), names the ceiling, and the one unbuilt high-leverage tool (a social-clip pipeline). The lens above sharing-channels + store-agents + tinyjam-marketing
│   ├── trailer-builder.md  BUILDING (2026-07-03): a humble, NON-DESTRUCTIVE CapCut over the .reel — the video-authoring surface for a multi-cart IAP app. Backbone SHIPPED (build-app-reel.js: manifest carts → clip per rack → compose → one reel, proven on Tiny Jam 3-rack); editor UI designed (Apps-tab "Trailer" section, horizontal timeline, transitions-at-joins, per-clip trim/speed, bake→preview). Principle: every edit is a .reel PARAMETER (ffmpeg trim/setpts/xfade on copies), sources never mutated, git = undo. Studied CapCut (steal timeline+joins+trim/speed; skip multi-track/text/effects → CapCut or engine-native)
│   ├── export-ratios.md  EXPLORING (2026-07-08): getting a cart's motion into the frame shape a CHANNEL wants — a target-ratio/resolution axis (9:16 TikTok/Reels · 16:9 · 1:1 · App Store device sizes), NOT App-Store-only. Orthogonal to the .reel scenario (one scenario → many ratios). Two approaches: (a) COMPOSITE crisp cart onto a target canvas (the store-shots.js stills trick, per-frame) — universal, bars/bg; (b) NATIVE capture at the target canvas (SCREEN_W/H + device-adaptive reflow) — fills the frame, reflowing carts only. Pieces exist scattered: store-shots composites stills, compose-clips already letterboxes reels to a # size, make-gif renders native. Gap = video composite-to-ratio. First step = make-gif --ratio + a Promote-tab ratio picker
│   ├── store-agents.md  IDEA (2026-07-03): the JUDGMENT layer above ADR-0026's store plumbing — the agentic work that decides whether an app gets accepted + discovered (hero-frame director, copy transcreation, ASO, guidelines pre-flight, whole-page stranger-legibility audit, rejection triage, review-response). Each split at the script/agent boundary (scripts prepare → agent decides → scripts execute); the one rule = if it's deterministic given inputs it's a script, the agent only does judgment/language/world-knowledge
│   ├── attract-mode.md  PROPOSAL (native-first): arcade "attract mode" — a cart replays a bundled demo track when idle, drops the replay on first real input and goes live; reuses the harness replay path (inject_input + inp_down raw read = the handoff); the attract track is EMBEDDED in the cart (de:demo zTXt), video tracks stay in tools/clips/; prototype native in sloop before any web embed. UPDATE (2026-07-05): a 4th, strongest payoff — the IN-APP IAP STOREFRONT: a LOCKED rack idles into its demo, first touch = "unlock to play" (arcade coin-drop → IAP); one de:demo track drives three surfaces (App Preview video · in-app locked-rack demo · web embed)
│   ├── resolution-portable-input.md  EXPLORATION (2026-07-05): where the replay system meets device-adaptive layout — a touch track is absolute canvas PIXELS, so it breaks when a rack reflows for a different device; keyboard tracks (and thus audio) are position-free and portable. The plan is KEYBOARD-FIRST (racks demo themselves via transport/preset keys → truthful full-bleed App Store videos at every size from ONE track, zero engine work), with the coordinate-portability systems (A normalized coords · B semantic widget-addressed input · C per-shape tracks) PARKED as escape hatches, not built upfront
│   ├── transitions.md   BRAINSTORM: scene-to-scene wipes — the transitions.c demo (iris/clock/wipe/curtain/blinds/dissolve/fade/slide, the "overlay a black mask whose shape = how-covered" trick) → a reusable transitions.h header; the SLIDE "both scenes co-resident" case is the design crux; keeps three things separate — A in-cart transitions (the goal), B build-time clip stitching (passive video), C a live cross-cart player (= attract mode across carts, stubbed)
│   ├── big-game-backlog.md  LIVING BACKLOG: what's left per cart (sloop/streetlab/roadlab/roadnet2/flank/thecut/lotfill/interiors/dialogue) + the cross-cutting SEAMS no cart owns (the world composer, the on-foot↔car avatar, money, a pressure clock, persistence) — the high-leverage "what to build next". Companion to showreel-teaser.md
│   ├── cart-metadata.md  DESIGN (2026-06, being built): each cart owns a `de:meta` block in its .c (a SUPERSET — index.json is one generated view); build-cart-index.js generates editor/public/carts/index.json from it, killing the multi-agent index.json conflict. Schema (status/description-as-string-or-parts/reserved inputs·outputs for the man-page+data-IO future), array-order-deprecated, the content-lossless migration. Implements field note 013
│   ├── action-plan.md   LIVING ACTION LIST (2026-06): the do-able asks from the field-notes journal + the Tinyjam brainstorms, prioritized & checkable. Tier 0 (save/share lz-string codec = the tinyjam racks seed-code; build-field-notes; rebake thumbs), Tier 1 knowledge infra (per-cart-metadata→generated index.json = THE keystone that kills the index.json multi-agent conflict; cart lifecycle/curated views; build-context; frontmatter; stale-doc/promote-candidate), Tier 2 GATED audio product (JSON-diff/RLHF export, bias knobs, visualizer, native iOS/AUv3), maker decisions (the Tinyjam naming, settled), and the systemic cart-polish backlog (shared touch-joystick/pixel-button/drag-capture, adventure z-order, end-state, text overflow) distilled from field-notes/cart-polish-punchlist.md
│   ├── showreel-teaser.md  BRAINSTORM: a teaser/trailer for the larger game (lo-fi GTA1, procedural world from sloop/streetlab/roadlab/roadnet/flank) used as a FORCING FUNCTION — a shot list that doubles as the prioritized gap list. v0 stitched-now vs v1 integrated; the real gaps are SYSTEMS (the world composer) not API primitives (lerp/clamp/camera_ex/collision already exist)
│   ├── traffic-ai.md  NPC driving as a TRAFFIC mode in trackgen (same track, not a race; a mode not a new cart). SHIPPED: lane-keeping + car-following (IDM), overtaking, a cycling traffic light + queues, phantom jams (reaction lag), oriented car collision — all spec-gated (24 assertions). NEXT (phased A–D): the cross-road intersection — a big L→R road cutting the loop ~twice with its own traffic + right-of-way. Then speed zones, hazards, pedestrians, two-way, green-wave lights, emergency vehicle. Feeds sloop rung-4 NPC traffic + streetlab's signal-state/ped-logic gaps
│   ├── cart-clips.md    CONVENTION: where baked clips (webm/gif) live — input track tools/clips/<cart>/NN-label.{script,beats,rec} → make-gif.js → editor/public/clips/<cart>/NN-label.webm (auto-detected by glob). The clip/showreel pipeline; notes the SILENT-clips audio gap (--wav exists, just unmuxed) + why clips are the cheap showcase win (ties to ADR-0020). Tooling how-to: guides/debug-harness.md "Clip capture"
│   ├── modular-synth.md  modrack design spec: Eurorack-style control-rate patcher cart (modules, cables, data model, shipped ledger)
│   ├── product-notes.md IDEA: the audio line as a (maybe) product — market thesis (Pocket Operators / Koala precedents), sketch-first decision, the parked builder items (MIDI tiers, AUv3/Link, native iOS) each with cost + un-park trigger, the web-latency measurement plan, the Roland-trademark paywall rule
│   ├── product-notes-followup.md  IDEA: a 3rd outside-agent brainstorm, delta on product-notes.md — the product-stage detail (ANSWERS its pricing-shape + feedback-channel open Qs). The "Tinyjam" naming tension 🚩, save/share via URL lz-string codec (= the seed-code, do NOW for the web gallery), native C+raylib→iOS port specifics (Metal/ANGLE, callback loop, sandbox paths), AUv3 multi-extension + App-Group entitlements, the free-hub+IAP+one-time-Master-Pass model, Module-of-the-Week remote config, the Xcode-free agentic Fastlane pipeline, in-house StoreKit 2 bridge, Android=later, Loopy Pro (ex-Audiobus) forum as the channel. Numbers are LLM-supplied — verify
│   ├── engine-portability.md  OPEN survey: the platform seam (window/input/audio/assets behind a backend interface so the engine runs host-less), the THREE iOS-enabling refactors + general health (split monoliths, prune A/B flags), and THE load-bearing undecided call — software canvas vs GPU as canonical renderer (gated on fps measurement; decides whether iOS needs ANGLE). Feeds an ADR once measured
│   ├── ios-plan.md     IN PROGRESS: the iOS build ladder + reusable scaffold (lives in /ios) — execution companion to product-notes-followup; settled forks (xcodegen, software-canvas render, hello-world first), the simulator-only spike ladder (toolchain ✅ → render → audio → save → StoreKit-local → App-Group → CloudKit → AUv3), and the ghera/ANGLE HW path investigated + deferred
│   ├── ios-profiling.md  SURVEY + build candidates: Apple's instrumentation reachable headlessly via xcrun xctrace (records + exports XML, so an agent can profile a cart on-device and report hotspots) — which templates matter for a software-canvas engine (Time Profiler #1, Audio System Trace, Animation Hitches, Allocations; skip Metal), os_signpost + lldb, and the tools we could build (profile-device.sh, engine signposts). Holds the first on-device bunnymark hot-function breakdown
│   ├── touch-controls.md  BUILDING: an on-screen joystick/d-pad/buttons, responsively placed into a deck (portrait)/rails (landscape)/corner-overlay (fallback) band via `gr_place()`, drawn by a per-platform window-space skin (pivoted off the original native-res-in-canvas plan). Shipped: `touch_layout(mode, n_buttons)` with all four move modes (`TOUCH_ANALOG`/`TOUCH_ANALOG_FIX`/`TOUCH_DPAD4`/`TOUCH_DPAD8`), band-relative control sizing (`ctrl_scale`), N action buttons (0-4, diamond-laid — grew the core input vocabulary with `BTN_X`/`BTN_Y`), a `touch_debug()` introspection API, real validation in 16 carts, a `touchvocab` reference cart, and a RAILS button-overflow fix (per-side `ctrl_scale`/`ctrl_scale_btn`, actual-footprint sizing, edge margin). Open: the editor's cart-reflective settings readout, iOS shell wiring, web deck/rails, twin-stick + 2-player layouts (deliberately deferred, seams noted)
│   ├── pixel-perfect-scaling.md  SHIPPED as an opt-in engine setting (`scaleFilter` 0-3, sharp-bilinear shader): scaling the low-res canvas to a non-integer window size without wobble (nearest) or blur (bilinear) — demo cart `pixelperfect.c`. See `window-fill-scaling.md` for the half of the original idea that never got wired in
│   ├── window-fill-scaling.md  IDEA (2026-07-01): the fractional "fill the window" step from the original pixel-perfect-scaling exploration never got ported — `gr_place()` (touch-controls' placement brain) always picks an INTEGER scale, so a resizable/web/fullscreen window is capped at the last integer step with dead space beyond it. Sketches the fork (fill fractionally only when no touch band is claimed) without designing it
│   ├── switch-target.md  IDEA/parked: Nintendo Switch feasibility — technically easy (the iOS AOT-cart + software-canvas work already retires the engineering risk; same no-JIT/no-GL-backend constraints), legal/logistical is the hard part. The two roads (homebrew devkitPro/libnx = portability proof, can't ship eShop · official Dev Portal = gated on approval, real target is Switch 2), hacking a unit (Erista RCM = easy/reversible vs Mariko = soldered modchip), and whether App Store presence helps the Nintendo dev-account application (a shipped GAME helps most; ship one cart-as-game = the pitch artifact)
│   ├── tinyjam-racks.md      IDEA: ReBirth-style genre racks (generate → play → export) — radios as song generators feeding editable sequencer carts; the lane format, seed-as-song-code handoff, song.h export for game soundtracks, the 8-rack genre table (incl. rebirth-classic, the RB-338 homage itself) + the wider map (unbuilt stations × engine wishlist, cross-indexed from future-stations/instrument-engines)
│   ├── rebirth-classic.md  SHIPPED (2026-07-02, design → cart in one day): the RB-338 homage rack (2×tb303 + curated tr808 + full tr909 in ONE self-contained cart) — the tinyjam-racks rebirth-classic row promoted after a research pass: slot math (~24 of 27, 808 curated), 320×240 accordion strips, banks A–D + chain row = real songs in v1, seeded acid generator (no seed-compat burden — no acid station exists), and the 303-filter fidelity spike (FILTER_LOW is a clean 12dB SVF; A/B vs LADDER/STEINER, FILTER_DIODE pre-approved if needed, navkit's unported oversampled ladder is the donor). Proposes taking the pilot slot from rebirth-house
│   ├── yacht-rack.md  SHIPPED (2026-07-02, design → cart same day): the session-desk rack — `yachtrack`, tinyjam rack #2, the first CHORD-FIRST rack and the first radio→rack seed handoff (yacht's new_song VERBATIM, gated by a 44-pair golden songsum corpus in spec()). Chart+interpreters model shipped as specced (chord row w/ the mu vocab + MU-IFY/SUS-MELT, form row + GEAR, drum skeletons w/ drummer chairs, the 32-step sax cell; bass runs/comp anticipation/ghosts/swing behind FEEL KNOBS — answers tinyjam-racks' humanize question). rack.h deferred at customer #2 (idiom, not code, is shared); the ~90-byte chart blob = song-codec's first customer
│   ├── tinyjam-racks-followup.md  IDEA: a 2nd outside-agent brainstorm, delta on tinyjam-racks.md — confirms the freeze-to-lanes spine independently; NEW: live "bias" knobs (drive the generator not the sound), the agentic RLHF loop (JSON before/after diffs + A/B-as-training-signal → agents tune the radios), a pattern-driven Game&Watch dancer + 9:16 social video export (sprite-draw + make-gif), and MIDI-in confirmed (→ product-notes parking lot)
│   ├── radiophonic-workshop.md  IDEA: the vintage-sci-fi box (BBC Radiophonic Workshop homage), promoted from the tinyjam-racks row after the 2026-07-02 drift pass found it engine-unblocked (INSTR_VOICE + ringmod + tape/grains shipped; leads exist three ways: heldnotes theremin / martenot / musicalsaw). The concrète rhythm design (polymetric unequal-length tape loops, ringmod'd lampshade ticks, BBC-pips hat, splice offsets not humanize), the maker's pieces-first build order (prove each new voice as its own small cart — tape-kit, ring-mod playable — then combine; live-looper flagged as an ingredient), and the station-vs-rack shape fork left open
│   ├── pocketbox.md  SHIPPED v1 (2026-07-02, design → cart same day): reimagining the Wee Noise Makers PGB-1 pocket groovebox ("between an instrument and a rack") — keep its buttons-only grammar (16 step keys + 4 modes + hold-combos + one touch strip + an honest 128×64 OLED window), translate its guts onto shipped engine surface (verdict: ZERO new engine features; the 2 sampler tracks become wave_set/INSTR_USER wave tracks, its 4 FX buses = per-slot drive/reverb/crush). 8 fixed-role tracks, p-locks/ratchets/Elektron trigger conditions, chord track feeding melodic tracks, 16 per-track patterns (polymeter), 12-part song mode, phased build order
│   ├── tracker-cart.md  SHIPPED v1: the Song→Chain→Phrase tracker cart (`tracker`, LSDJ/Dirtywave-M8 lineage) — fills the shelf's song-arrangement gap without reopening ADR-0003 (it's a cart, not editor UI). The five M8 lessons (nested phrases w/ chain transpose, single-track phrase view = the 40-col answer, one-screen-per-view spatial nav, 3-letter mnemonic FX not hex, cursor+nudge modeless editing), a ~5 KB save_bytes document that doubles as song-codec's future lanes layer, mnemonic FX mapped to existing note_* API (zero engine work), and an intent-first 8-preset band. Complementary to the racks (document-first vs generator-first). Still open: tables, live mode, the song-codec blob
│   ├── song-codec.md   IDEA: how a cart serialises a song for share/save — the seed→lanes→events fidelity layers; why a u32 seed code is universal across all 34 radios for free but stops sufficing the moment a user authors. the sharing channel is the URL (`?seed=…`), not an in-app text field. Show-code already ships (bare 8-hex u32); the keystone gap is reading `?seed=` at boot (one line in radio.h via a tiny start_seed()) + a copy-link button. Decision: keep the bare seed now, add a length-distinguished `?song=` blob ONLY when the rack's edited lanes need it (so old links never break); grow-as-we-go
│   ├── blend-tables.md  exploration behind STATUS item 18: index-only translucency/glow/shadow; cart-space prototype = blendlab.c
│   ├── palette-and-color.md  exploration: own palette vs the borrowed PICO-8 32 — one decision wearing two questions, sequenced before blend tables bake it in
│   ├── dynamic-palettes.md  IDEA (thinking-in-progress): the deep version of palette-and-color's Layer 2 — make the palette per-cart/swappable (the index becomes a ROLE, not a colour). Resolves the hard part: the CLR_* constants become role names, kept honest by palettelab's role-mapping (CLR_RED = "this palette's red", carts reskin free). The three-way space (fixed/dynamic indexed/truecolor), the machinery already exists (palette_hex/pal/blend-rebuild = palettelab is the proof), the "a cart names its own few colours" variant, the honest off-role residual (no grey in Rosy, no teal in DB32), and the opt-in/back-compat path
│   ├── physics-notes.md  brainstorm: a tiny in-engine physics layer? (sparked by ragdoll.c; nothing decided)
│   ├── boom.md           LIVING WISHLIST for boom.c (the destruction petri dish): the two honest layers (heat vs pressure), what shipped (6 blast tools, reactive props, destructible walls → physics blocks), and the ordered next list (structural collapse → blasts shove loose objects → feed demolition back into sloop → pedestrians → drivable car). Prototyping ground for sloop's demolition rung
│   ├── probe-carts.md   LEDGER (kept current): which cart was built to answer which API question, and the verdicts
│   ├── cart-library-direction.md  snapshot of the ~201-cart shelf + opinionated "what carts to build next" (tutorial on-ramp, more toys, the few missing teachable games; NOT more clones)
│   ├── teaching-gaps.md       what the API offers but no tutorial teaches — mouse/camera/audio-fx/touch/3D gaps, ranked; measured from cart-index.js + api-usage.js; the attack list for the on-ramp
│   ├── tutorial-curriculum.md  plan for the next wave of tutorial carts (25+): the language-fundamentals / collision / whole-game-capstone tracks, adapted from the Nerdy Teachers PICO-8 course (adapt the arc, rewrite in C)
│   ├── cart-survey-api-priorities.md   cart-evidence-first memo: what real carts prove, filtered through existing decisions
│   ├── api-usage-audit.md   which API functions the carts actually use (least-used tail, the 11 never-used, doc-coverage gaps); re-runnable via `node tools/api-usage.js`. Marked `de:driftable` (see driftable-docs.md)
│   ├── driftable-docs.md   the `de:driftable` convention: docs that freeze a tool's numbers DECLARE it, `stale-doc-check --driftable` + a cart-status advisory flag when the source moved. Plus the generate>execute>lint>register>accept hierarchy for keeping derived numbers honest
│   ├── baked-rotation-atlas.md   pre-rotated sprite/shape atlas: fast blitted bodies, the centerline model, offscreen canvases
│   ├── font-rendering.md         playful text beyond print: shadow/outline/wave/typewriter, inline codes, PICO-8 comparison; + 2 baked tiny fonts (3×5/4×6) awaiting print_small/print_tiny wiring
│   ├── rasterization-consistency.md  OPEN: one consistent fill/outline/dither rule so shape edges always agree (seams, off-by-1, outline mismatch)
│   ├── software-canvas.md  PROPOSAL (the fleet survey's #1 lever): rasterize the whole 64k-px frame on the CPU + UpdateTexture once/frame to kill rlVertex3f engine-wide (pset is 59.6k/frame); the design forks (RGBA vs index buffer, camera_ex zoom/rotation, compositing) + a staged DE_SOFTWARE_CANVAS prototype plan. Primitive-level questions all de-risked by tools/det-probes/ (determinism, tritex, lines/caps, rotated fills/strokes/sprites+RotSprite)
│   ├── software-canvas-phase0-plan.md  READY-TO-RUN plan: the GO/NO-GO mechanism probe — wire cbuf+UpdateTexture into studio.c:1354-1365, route pset/rectfill/fills/sline, defer print; byte-identity gate on a line-free cart + perf gate on cityplan (~20ms→~7ms). The one experiment left; touches hot shared studio.c
│   ├── geometry-helpers.md  PROPOSAL: graphics taxonomy (which carts draw with circles/tris/dither/sprites/gradients) + 2D drawing primitives for the geometry-first style (ngon/star, poly/polyfill, gradient, rounded-rect, thick-line) ranked by cart boilerplate; also parks a thought on real palette color-lerp (lerp_color/rgb true-color) with the second-thoughts against it
│   ├── second-north-star.md  REFLECTION (philosophy companion to VISION.md): the honest, adult pursuit grown alongside "learn C, make things" — deep simulations behind a humble lo-fi/SNES surface; the legendary-carts thesis (one honest core, the second face); audio as the same soul at scale; why the combination is load-bearing; the honest tensions/forks
│   ├── galerijflat.md   IN PROGRESS (multi-session): design seed for an experimental/arty cart — the Dutch gallery-access apartment slab as a generative tableau of ordinary life (gallery facade straight-on, the archetype; the facade as a pixel grid of households)
│   ├── data-layers.md  DESIGN SEED: the SimCity-2000 aggregate tile-data SIMULATION (the consequence side, mirror of procgen-places' generator) — given terrain + tile roads + painted RCI, COMPUTE the emergent layers (land value, crime, pollution, density, service coverage) via two mechanisms (cross-layer formulas + diffusion smoothing); the dependency DAG + formula shapes + the crime↔land-value slum spiral; cart gridcity, v1-bare→full plan
│   ├── driving-world-program.md  ★★ THE UMBRELLA over the whole "build a vehicle, drive a procedural world" research (lo-fi GTA1) — maps EVERY cart+doc across 7 layers (movement/sloop · world-spine/roadnet(2) · road-geometry/road-lab+street-lab · city-content/cityplan · rendering/cityview+citydrive · OSM-data/roadview · traffic-ai) with per-layer status, the 3 convergence SEAMS, the REAL-IN/SYNTH-IN/one-render convergence (roadview+citydrive ↔ streetlab+roadlab — the two ends grow into a common centerline+junction-grammar model), and a prioritized "finish the right stuff first" call (P0 = define the spine AS that common model, mining roadnet/roadnet2 for their unique terrain-generation pieces; P1 = wire road_at()→sloop). Sits ABOVE road-program-state.md; start here for the cross-layer view
│   ├── procgen-places.md  PROPOSAL: vary procplaces' roads-&-cities generator — split the one density field into two orthogonal fields (land-use R/C/I+G × intensity light/med/heavy, SimCity 2000 model), road-class-by-hierarchy, domain-warp to de-grid; the draw/road_at() shared-geometry rule that keeps sloop safe; v1/v1.5/v2 scope (arced roads = the v2 wall)
│   ├── roadnet.md  IN PROGRESS (cart roadnet.c): the infinite/deterministic SPLINE road world that breaks procgen-places' "arced roads" wall (node-lattice splines are local) — ranked hub/town hierarchy, terrain-aware bend/bridge, + the L2 street level in the magnifier (blocks/lots, grid aligned to arterials, class-aware carved roads, outskirts gradient + Voronoi fields) exposed via the road_at() query seam
│   ├── roadnet-handoff.md  ★ START HERE for roadnet — orientation: what's built, the LOD stack (L0 map → L2 street → L3 to come), commit trail, key invariants, and the recommended NEXT-TIME order
│   ├── roadnet-streetlevel.md  DESIGN SEED: L3, the on-the-street level (the missing access-street tier + building footprints, where sloop will drive) — the seams that keep it deterministic + the deeper "BLOCK" loupe harness; has a "starting point for next session" note
│   ├── streetlevel-content.md  ★ the FILL LANGUAGE that populates the partition (and the land/wilds outside cities) — built in cart lotfill.c. The taxonomy ("kinds at a glance": atoms vs biomes/recipes/flavors/fields/modifiers/process/composition + a litmus test), the phased PIPELINE (fields→partition→makers→modifiers), the 7 built atoms + their flavors, the proposed set (flow/process atoms, fertility/taint fields, ruins = collapse→overgrow), connectivity = READ-seams-not-generation (road_at stub → roadnet swap), and the city integration-spec vs buildable-now split
│   ├── cityplan.md  ★ the lotfill + interiors MERGE — built in cart cityplan.c: a whole world (terrain/forest/farm/city) you LIFT THE ROOF off by zooming (city of roofs far → BSP floor-plans near → furniture closer; F = dollhouse). The scale decision, the compose order, and the staged build plan — now carries EVERYTHING both parents had (A terrain+scatter+rows ✅ / B ruins ✅ / C border-pave-stamp ✅). The cart we develop forward; lotfill/interiors are now reference workbenches
│   ├── roadnet2-plan.md  the vector-native rebuild plan (one graph, one road_at query); Part B = hierarchy + INTERCHANGES (the junction matrix), which spun off the road-geometry thread below
│   ├── worldgen-plan.md  ★ READY TO BUILD (2026-07-06): the REALISTIC-roadgen plan — closes the umbrella's named gap ("lattice+jitter reads procedural, not grown"). Inventory of what exists, the 5-point gap, the method calls (tensor-field arterials per bounded city = chunk-safe determinism; Parish-Müller's density-map split without the L-system; streetlab's 5 patterns as morph-knob presets; SNDi-vs-OSM as the realism number), and a gated rung ladder 0–7 (rung 0 = tools/sndi-check.js, the metric oracle over real .rvb cities AND generated graphs → the per-pattern target table; rung 4 = per-district minor fill, where "procedural grid" dies). Expands driving-world-program.md Track A (A2–A4)
│   ├── roadnet2-handoff.md  roadnet2 cart state / orientation
│   ├── road-program-state.md  ★ WHOLE-PROGRAM STATE on one screen: every road tier (interchange/at-grade/network) + cart + build status, what's shipped (streetlab M1–M7 + the Stage-2 network superblock + circuity, the spec() harness), and the open frontier (the modal active-travel layer, the two-tier generator, a seed-driven world). Start here for "how far are we"; the docs below are tier detail
│   ├── roadkit.md  ★ READY TO BUILD (2026-07-01): the decision + plan to extract the shared road grammar into runtime/roadkit.h. Answers "when does streetlab's beauty (curb returns, pavements, markings) reach the DRIVABLE world?" — key insight: at fast top-down driving zoom the grammar is sub-pixel/invisible, so it lands in **citydrive FIRST** (the pseudo-3D real-OSM view — the close/pitched camera where it's actually visible), NOT sloop. streetlab stays the spec-locked source; sloop's rig hooks in later over the shared road_at() seam. Trigger fired (OSM consumers + the N-arm-native renderer is the needed piece). Spec-gated phasing (pure-geometry extract keeps spec 104/0). Supersedes road-program-state.md's "no roadkit.h yet"
│   ├── streetlab-corner-symmetry-plan.md  BUILT+verified: streetlab's ≤1px arc-flip corner floor FIXED on symmetric 4-ways by mirror-blitting the rendered quadrant (mirror_blit, pget/pset, guarded !isT && skew==0 && !freeRight) — kerb 7→0 via tools/mirror-diff.js. KEY FINDING: the fill was already symmetric; the floor was the kerb STROKE (GPU line); a symmetric line (sline) can't fix it (fill=point-mirror vs stroke=cell-mirror snap conflict) so the blit is the fix. sline banked as the software-canvas CPU-line. Probes: arcsym, linesym, axissym. mirror_blit/sline are API candidates
│   ├── field-based-road-rendering.md  SHIPPED in streetlab as OPT-IN ('g' key / -DDE_FIELD_ROADS, default OFF): draw the road from ONE coverage field (latDist = ⊥ dist to nearest centreline) — asphalt/kerb/sidewalk/lane-lines all thresholds on it, uniform + symmetric at any SKEW/CURVE, fixing the per-arm strays/missing-outline/double-lines. Whole junction covered (plain/skew/T/turn-lanes/cross-section/roundabout/free-right); network view stays per-arm. Boundary: render RULES port verbatim, field SOURCE is per-cart, dashes/arrows stay a separate paint layer. Gated by tools/road-check.js (correctness oracle). Old route (mirror_blit + casing-fillet + stroke_corner) stays the DEFAULT until software-canvas.md makes the per-pixel fill cheap, then it gets deleted
│   ├── road-geometry-handoff.md  ★ START HERE for interchange/junction GEOMETRY — master map of the effort: carts interchange → rampkit → roadlab, what we're solving, the research, what failed, how to continue
│   ├── interchange-handoff.md  the interchange.c cart state (trumpet loop work, the junction matrix, isolation-bake workflow)
│   ├── interchange-dsl.md  the junction DSL: topology layer (legs × movements × ramp-primitive) + geometry as relations/solver (the "language that isn't math")
│   ├── road-geometry-refs.md  RESEARCH: OpenDRIVE / clothoids / SUMO / Cities:Skylines / curve-offsetting — recommends an arc-spline (LINE|ARC|CLOTHOID) model; arcs nest concentrically for free
│   ├── junction-lanelink.md  the junction DATA MODEL (OpenDRIVE junction/laneLink ↔ our DSL) + the roadlab build log: schema (M6), the loop/flyover/generator specs (§8), the (topology × policy) plan + skew finding (§9) — roadlab now generates the whole roads.org.uk catalogue
│   ├── road-hierarchy-notes.md  RESEARCH (2 deep-dives, fact-checked): where roadlab sits in the FULL functional road hierarchy — the at-grade junction primitives + the local/collector NETWORK-TOPOLOGY patterns (SNDi metrics, Parish&Müller / tensor-field procedural gen) we have NOT modeled; the unmodeled frontiers below the interchange tier
│   ├── roadlab-method.md  the replicable WORKING METHOD behind roadlab (standard-first → milestone ladder → bake-and-look → verify gates → commit-per-milestone → docs-as-spine) + how the next phase reuses it; read before starting the next frontier
│   ├── pseudo-3d-city.md  the cityview render bench (cart cityview): the GTA1-meets-Zelda parallel-oblique projection (ground = rotated plan map, height pushes straight UP screen), the one invariant (a wall's UP stays screen-up; diagonal footprints are fine), the four building view modes + the heading-up finding, tritex wall textures, drivable raised-highway FLYOVERS (z-profile decks + pillars + fascia + camera-rise), and the ADR-0021 projected-primitive helpers (project/pdisc=ground-circle→ellipse/pline+pdash/pproj_poly). The mechanism behind decision 0021; next = pipe roadlab's real z(s) deck through it
│   ├── menigte.md  EXPERIMENT (v1 cart shipped): the SCALE axis of "drive a large world full of meaningful NPCs" — 10k people, all always-live, because state is a pure-function-of-(identity,time) closed-form schedule (cheap, all 10k/frame) while only the on-screen few pay for walk-anim/steering; no hydrate/dehydrate; the HOT-tracks-closed-form invariant (the procplaces shared-geometry rule, one level up); companion to procplaces (world) + sloop (car)
│   ├── sloop-production.md  DESIGN + EXPERIMENT (v1 cart "The Cut" / tools/carts/thecut.c shipped): the MONEY axis of sloop — a Factorio/Schedule-1/Dwarf-Fortress-style "optimize a process for best quality/most output" subsystem; the optimization-itch principles, the "first you do it yourself, then you manage" arc + the DIY→manage bridge (one pure function, two drivers), and the v1→v3 staging (v1 The Cut bench → v2 delegation in place → v3 sloop integration)
│   ├── sloop-handwork.md  DESIGN + EXPERIMENT (v1 cart tools/carts/handwork.c BUILT): the EMBODIED half of sloop production — the CDDA body-in-the-world handling loop thecut skipped (gather → carry in a container → work-that-takes-real-time at a bench → load a machine and walk away → serve buyers), navkit's item/work-tile/passive-timer model minus the colony AI plus one walking avatar; the load-bearing insight that a MACHINE is your first delegate (one pure transform, three front-ends: you+focus / machine / follower); the OPPORTUNITY-COST hook that dodges the boring "grind→buy→automate" loop — two lanes (timed HANDS make FINE/premium, machine stamps COARSE/bulk, both demanded) + buyers who WAIT at the door and leave, so the machine is a relief ("can't be in two places") not a win; v1 slice + "far sight" ladder (stockpiles → growing+day-cycle → hand-or-machine chains → followers → machine tiers / richer demand via thecut)
│   ├── headless-autoplay.md  growing the debug harness toward windowless fast runs + agents that play to find bugs (navkit-informed)
│   ├── spec-harness.md  per-cart `spec()` — the gameplay twin of tune-check, a deterministic headless logic safety net (turns ad-hoc throwaway verification into a committed re-runnable artifact). **v1 BUILT** (`node tools/spec.js`; `step()`/`expect()`/`key_down` under -DDE_SPEC; API in runtime/spec.h; streetlab is the first/reference cart). Locked decisions (imperative, logic-only, opt-in/manual); deferred: frame()+probe for procedural carts + the zero-authoring golden check. Which carts warrant one via `node tools/cart-analyze.js` (complexity × global-state ranking)
│   ├── multiplayer-research.md  netplay for native C + wasm. **Rung 1 BUILT** (2026-07-02): UDP input-lockstep, localhost/LAN by IP — `runtime/net.h`, `--net-host`/`--net-join <ip>`, carts network-unaware, demo+desync gate `play.js <cart> netdemo`. Still research: WebRTC DataChannels (libdatachannel/datachannel-wasm) as the one cross-target transport, join-code UX + LAN discovery, iPad path, friendly net_host()/net_join() API sketch, rung ladder
│   ├── multitouch-and-generalization-audit.md  AUDIT (2026-06-13): all 85 instrument carts swept for mono-vs-poly, which carts read only one touch but shouldn't (upright bass double-stops, tr909 XY pad, glassharmonica chords…), and the hand-rolled plumbing ripe for a shared header — top win `runtime/pointer.h` (the 10-finger Ptr pool copied into 15+ carts)
│   ├── touch-notes.md  multitouch: the touch API + identity model, the release API (tapr/touch_ended_*, shipped with the touch piano), the never-wrap-rgestures verdict + gestures.h, the touchlab on-device checklist, §7: the web phantom-touch fix (DOM mirror, DEVICE-PASSED), and device finding #5 (iPhone's 6th finger mass-cancels ALL touches)
│   ├── mobile-web-notes.md  desktop carts meeting phones: device findings (5-touch iPhone ceiling, tiny text), mobile-lint + gallery badges, screen-fit policy, fullscreen/PWA install, §4c responsive display (resizable window / scale-to-fit / rotation), §5: the device-facts trio proposal (touch_available/device_class/touch_ceiling + the ceiling-safeguard pattern, iPad-pretends-to-be-a-Mac detection traps), and §6b the keycap (tappable-key-label) proposal
│   ├── keycap-audit.md  AUDIT (2026-06-13): all 348 carts swept for mobile-touch readiness, centered on the keycap pattern (mobile-web-notes §6b) — per-cart verdict (touchControls-flag 114 / keycap 100 with 331 extracted chips / none 98 / touch-first-redesign 18 / wontfix-desktop 18); top win = one `radio.h` change makes 24 radios' R/[/]/M tappable at once. Re-runnable via the audit workflow
│   ├── responsive-layout.md  BRAINSTORM: a four-primitive layout set (flex / fluid clamp+% / anchor+inset / aspect) for the hard "layout-responsive" half of mobile-web-notes §4c — prototyped in cart-land first (tools/carts/respond.c: drag a fake screen's corner, watch it reflow) before SCREEN_W/H ever become runtime vars; candidate runtime/lay.h API + graduation path. The maker's call (2026-07-02, see window-fill-scaling.md): the likely-better route for iOS/the sellable product over fractional display-scaling
│   ├── acidrack-layout-brief.md  DRAFT (2026-07-05): the first per-rack layout brief (device-adaptive R1) — acidrack's three-state strips (folded/compact/expanded), arrangements per shape, editor-swap table, done-bar; §2's compact-strip taste calls await the maker
│   ├── acidrack-ui-research.md  RESEARCH (2026-07-07): the external survey behind the brief — how the real TB-303/TR-808/TR-909 and the best clones (ReBirth, Phoscyon 2, ABL3, Roland Cloud, iOS acid apps) lay out, the touch/density numbers (48px floor, 1×16→2×8→4×4 fold, band table), and a map from the evidence onto the brief's §2 open taste calls. Ammunition for the calls; the brief owns the decisions
│   ├── epiano-layout-brief.md  DRAFT (2026-07-07): the SECOND per-rack layout brief (single-instrument keybed shape, vs acidrack's rack) — the FAITHFUL epiano Phase-3: the classic chromatic piano keybed that scales with device width + a disclosing sound panel (INLINE iPad / COMPACT+FX-tab phone), NOTE/SOUND split, finger-gated keys (≥0.9 finger, measured). Re-scoped 2026-07-07: the scale-grid feature moved OUT to scale-grid.md (epiano keeps its piano; the grid is an optional future editor — both eventually). Built by responsive-instrument-ui.md (its step 3), mock = epianofit
│   ├── scale-grid.md  PROTOTYPED (2026-07-07): a scale-locked ISOMORPHIC pad grid — SHIPPED as the scalegrid cart (device-tested, spec 71/0). Pick scale+key → in-scale-only pads, no wrong notes; 11 scales incl. blues + FOREST/SoundForest; ROW = OCT↔4TH toggle; SQR↔HEX packing (equidistant Tonnetz grid); fill-both-dims finger-first + SIZE cycle; VOICE cycle (PD/EPIANO/MALLET/ORGAN/PLUCK). "Where does it live" ANSWERED B→C: built as its own cart, grid maths self-contained; OPEN STEP = extract into a grid.h library (reuse solo.h) then wire epiano's editor-swap. General note surface, not epiano-specific
│   ├── device-matrix.md   REFERENCE (2026-07-07): the carried resolution matrix — ONE canonical place for two questions we keep re-asking. §2 = the device SHAPES a responsive cart reflows to (design-against, logical @ K=2, grown to 11 incl. iPhone SE + iPad mini; regen via play.js --resize); §3 = the App Store asset PIXEL sizes we must ship (the "at least" set: 6.9" iPhone 1290×2796 + 13" iPad 2048×2732, grounded in store-shots.js). Layout briefs + the store pipeline both point here instead of re-tabling
│   ├── device-adaptive-layout.md  BUILDING (Phases 0–2 DONE; Phase 3 RE-PLANNED 2026-07-05): the engine + product plan that graduates responsive-layout.md — make SCREEN_W/H live-resizable + PHYSICALLY sized (points, not px) so one cart is beautiful on iPhone AND iPad, both orientations, driven by finger/screen ergonomics (density × orientation → a handful of fluid arrangements, not N pixel layouts). One foundation, three payoffs: the product bar, mixed-res racks in one app, and honest full-bleed App Store screenshots + videos from one cart. Engine foundation shipped; Phase 3 (per-rack redesign) now runs brief-first: layout brief → runtime/disclose.h (three-state strips, per the ReBirth study) → finger_px() → ui-audit judgment layer → re-land acidrack (see §"Phase 3 — revised plan" + field note 018). Supersedes share-panel.md #3's "deferred" + answers ios-plan.md backlog #2
│   ├── frame-pacing.md     EXPLORATION: keeping phones cool — why a pinned 60fps cooks the device, Lever A (per-cart fps cap; the web emscripten_set_main_loop gotcha), Lever B (on-demand/reactive rendering + repaint(), the pause-freeze precedent, why not frame-hashing); the two compose
│   ├── editor-cart-workflow.md  the hand-editing gaps: no save-in-place, the sprite story (two sprite sources of truth, STATUS item 23), gallery metadata unreadable/uneditable — sliced proposals + priority table
│   ├── editor-scopes-and-facets.md  DIRECTION CHOSEN (2026-07-05 explore → 07-07 resolved): the editor's IA seam, resolved into DREAM → MAKE → PROMOTE → SHIP (a cycle). Make = existing code/pixels tabs, Ship = existing share popup, and the only verb with no home is PROMOTE (replays/clips/shots/trailer/tribes — a browse-and-assemble library). Scope is EMERGENT (a cart is opened, an app is selected — you never edit an app, you promote its set); apps consume cart-Promote data bottom-up. Right-sized: the verbs are a LENS, not a top-bar — the payoff is ONE new Promote tab. Dream = the zeroth verb (the authoring firewall), not a tab. → promote-tab.md for the contents
│   ├── promote-tab.md  READY (2026-07-07): what the per-cart Promote tab CONTAINS — one scrolling panel (like Apps), sections in produce → assemble → reach order: A clips&replays (takes+replay+record+bake) · B stills · C trailer (compose-clips) · D reach/tribes (reuse the 📣 leads glance) · E gallery-link + outreach board. MVP = D+A+E (mostly wiring built pieces); everything shown already lives on disk (cart-clips.md), so it's a VIEW problem. Falls out of editor-scopes-and-facets.md
│   ├── squishy-lines.md  READY TO BUILD: a velocity-brush drawing cart (320×320, 2-tone first) whose hand-inked strokes are stored as DATA, so an opt-in "boil" re-renders them with jitter into a 2–3-frame living loop — the draw-it-yourself, animates-for-free alternative to the AI-image→pixelsnap icon route
│   ├── ui-widgets-notes.md  PROPOSAL: runtime/ui.h cart-land widget kit (button/slider/knob/panel/drag-from) that works for mouse + touch + keyboard/gamepad — capture model, value-address identity, focus ring; second customer = the sfx generator's 17 sliders
│   ├── profiler-extensions.md  PARKED: first-frame/init timing + region "zone" timers (extend the in-engine profiler; reuse perf.json)
│   ├── press-kit.md  BUILDING (2026-07-03): v0.1 SHIPPED (tools/press-kit.js) — an in-house presskit()-style press page (factsheet/description/history/screenshots/trailer/quotes/monetization-permission) GENERATED from cart data: apps/<name>/app.json + apps/<name>/press.md + studio.md (prose in markdown, structured in flat frontmatter) + store-shots/make-gif assets → self-contained site/press/<name>/ + assets zip. Mirror the journalist-recognised format, build our own (same call as ADR-0026/Fastlane). Channel A artifact; shares asset generators with store-agents.md
│   └── packaging-distribution.md  NOT STARTED: shipping to non-dev machines — the dev icon/name stopgap, what real packaging needs (bundler/signing/notarization), and the real blocker (the ▶ run model needs a compiler; web/wasm is the likely public path)
├── marketing/         go-to-market — one subfolder per product (split out of design/ 2026-06-29)
│   └── tinyjam/        the Tinyjam product launch playbook (foldered 2026-06-30)
│       ├── tinyjam-marketing.md  the GO-TO-MARKET plan: one tribe per module — who loves each instrument/genre, where they gather (verified channel directory §3.9), the two/three tinyjam shapes, the portfolio→tribe→hook map, the cozy cluster + Adventure-Time reframe, launch sequencing, the funnel, video-as-amplifier, the launch kit (§7, incl. the own-palette App-Store blocker), how-to-show-up (gift-not-ask + the Minecraft arc + promo-code grandfathering)
│       ├── app-store-listing.md  DRAFT (US-first): the concrete App Store METADATA for the Tinyjams umbrella — title/subtitle/keyword field (lint-clean, grounded in a live aso-research run) + the per-module IAP naming scheme (each promoted IAP = its own search surface). Strategy: soul in the visible fields, generic instruments in keywords, module-specifics in IAP names; head terms (music/beat maker) are unwinnable, the tribe term (pocket operator) is the prize. Method: design/store-agents.md §ASO
│       ├── tinyjam-launch-strumharp.md  DRAFT launch copy for Strumharpy (omnichord) — the first Stage-1 kit: positioning + the harpy mascot, the de-clichéd Facebook + Loopy Pro posts, wishlist copy, verified channels, build-task checklist
│       ├── tinyjam-cozy-adventure.md  PARKED module seed: the "cozy adventure" rack (cozy-cluster centrepiece) — omnichord shimmer + chiptune + cozy ukulele-folk + bells; evokes the cozy cartoon-adventure vibe WITHOUT the IP; gated on Strumharpy landing
│       ├── research-insights.md  the LIVING distillation of the raw/ threads (the research routine): CHORDESTA (vibe-coded → flamed: the burned pitch + AI-tells to dodge, transparency) vs OH-84 (deep expert → near-silence: credibility ≠ reach, the authentic-musicality quality bar), the maker-voice/AI-disclosure stance (agentic = no hand-built claim), the crowded competitive landscape (minichord/Chordion/OH-84/OH-27/MidiStrum), contacts (SabreSour/OmnichordContentHub; Omnichord Heaven = competitor)
│       ├── omnichord-sound-model.md  what to mimic for an authentic omnichord: the square→lowpass→VCA signal chain (OM-84 = an 82C54-class PIT clocking square waves), the slow chorus/vibrato shimmer, the touch-mapped strum, and the fudged-triad + F#-note-wrap voicing players check for — all on existing engines, no new DSP
│       ├── icons/          app-icon art — tinyjam-icon-illustration.png + tinyjam-icon-pixel.png (sources), plus snapped/ = pixelsnap.js cleanups of the AI pixel icon (grid-snapped, posterized, pico/dither variants)
│       └── raw/            raw research threads kept verbatim-ish — omnichord-chordesta-thread.md (r/synthesizers) + omnichord-heaven-oh84-thread.md (r/Omnichord)
├── guides/            how-to
│   ├── cart-authoring.md         the make-cart.js / tools/carts toolchain
│   ├── cart-authoring-prompt.md  reusable AI prompt for designing a new cart
│   ├── AGENT-MESSAGE.md          parallel-build add-on: paste into each agent session alongside its brief + spec
│   ├── BATCH-2.md                batch-2 cart index (16 games built via Workflow)
│   ├── BATCH-3.md                batch-3 cart index
│   ├── cart-specs-index.md       batch-1 spec index + assembly recipe + parallel-build workflow
│   ├── cart-specs/               per-game cart specs (individual game files, not shown in sidebar)
│   ├── radio-station-howto.md    ★ START HERE to build a new radio station: the ordered runbook (firewall → design blind → shop palette → chassis → brains → build → register), a spine that links each deep doc at the right step
│   ├── responsive-instrument-ui.md  ★ the ordered playbook for a new rack's RESPONSIVE UI (sound→inventory→steal-IA→tier→brief→prototype→sweep→hands→ship): the sequence + gates + the field-note-018 traps, tying together cart-authoring-prompt / acidrack-ui-research / the layout brief / lay.h / the oracles. Answers "in what order do we do what to get a good responsive UI"
│   ├── interactive-wireframes.md  how to build a CLICKABLE, device-honest wireframe as a CART (the prototype step of responsive-instrument-ui.md): fixed-window + de_resize shape-flipping, the honest finger unit + finger-grid overlay, the safe-area skin, an immediate-mode clicked(box) touch/mouse layer (+ keyboard mirror for scripted renders), and the CLI verify loop. Worked example = tools/carts/acidwire.c; the gotchas that bit it (uppercase keycodes, gr_place DECK-pin, the `hit` symbol clash)
│   ├── instrument-map.md         ★ the museum map: the HUB over every instrument doc — which file answers what (shelf / recipes / presets / engines / backlog) + the hardware WINGS inventory (homages by manufacturer: Roland complete, Korg thin, Casio begun…) + the sampler doctrine pointer
│   ├── instrument-carts.md       the sound-toy shelf: every synth/machine/station/showcase cart, grouped by the building block it copies (radio.h / held-notes / ui.h / INSTR_*) — browse one to play, or find the closest relative when building a new one
│   ├── instrument-recipes.md     the supply-side palette: every grabbable instrument recipe across the sound carts (showcase presets, Roland drum voices, whimsical patches), organized by ENGINE (107 recipes); the "which recipe" companion to instrument-carts.md's "which cart"
│   ├── effects-recipes.md        the effects cookbook: named, copy-paste settings for every engine EFFECT (echo/reverb/chorus/flanger/tape/wah/drive[+modes]/bitcrush) with the showcase cart + used-by list — the effects analog of instrument-recipes.md
│   ├── instrument-presets.md     the named-patch catalog: every radio station's voice recipes given clear names, with sharing tiers (shared/variant/cousin/kin) so copy-propagation across stations is visible; paired with radio-voices.md
│   ├── porting-from-navkit.md    how to faithfully port an instrument/effect from the navkit sibling synth: port the oscillator VERBATIM (oscillator-first, not piecemeal), the gotchas (velocity×2, the ratio blend, amp normalize, always-on filter, drive-scale mismatch), macro mapping, the clav worked example + a checklist
│   ├── radio-voices.md           per-station voice charts (slot → role → preset name) + the findings summary (reuse clusters by idiom, the synth-kit/bass extract candidates, the 5 showcase-cart preset lineages, upgrade candidates); the reading view over instrument-presets.md
│   ├── exporting.md              ★ the five ways OUT of the editor (signed Mac .app / Windows .exe / iPhone deploy / publish to site / local web preview) — what each button produces, requirements, which tool owns the detail
│   ├── sharing.md                ways to publish finished carts (the web/URL flow — exporting.md #4 in depth)
│   ├── checks-and-oracles.md     ★ REVERSE INDEX: "I changed X → run Y to prove it." Routes a change (render primitive / perf / audio engine / cart logic / docs) to the matching deterministic gate — the task→tool map the flat tool list lacks
│   ├── debug-harness.md          deterministic record/replay/script + trace + live on-demand inspect + WAV capture/analysis + UI audit, off-screen text/overlap/hidden-panel finder (tools/play.js, tools/wav-analyze.js, tools/ui-audit.js)
│   ├── profiler.md               one-click CPU profiler (⏱ profile button): hot functions + call paths, frame budget, draw-call counts
│   ├── engine-optimization.md    the safe loop for making the engine faster WITHOUT changing output (stress cart → byte-identical dump oracle → keep-both env-flag A/B → perf.json measure) + the ledger; worked example = the polyfill scanline span fill (DE_POLY_FILL=legacy)
│   ├── live-coding.md            how-to for the live (libtcc) run mode: edit-while-running, hot reload, STATE/S state that survives reloads
│   ├── game-music.md             generative soundtrack recipes: step clock, chord brain, voice leading, time feel, seeds, brain catalog, radio.h; worked example = bossa.c (station candidates live in design/future-stations.md)
│   ├── voice.md                  Nikki's writing voice (synthesized from mipolai.com) for hand-written editorial prose — first customer = the history page's editorial seam
│   └── history-page.md           ★ how the generated history page works + how to ADD A WEEK (the build-history.js generator + the history-spine.json structure)
├── history-spine.json hand-authored STRUCTURE for the generated history page (weeks, eras, subsystems, importance tiers, marked days, milestones, editorial seam); dates/commits/ADRs/carts/heroes are derived from git
├── history.html       GENERATED project-history timeline (tools/build-history.js) — a neo-brutalist magazine read, grouped week by week; opened via the Docs tab's "★ history" item. Covers weeks 1–3; how-to + add-a-week → guides/history-page.md
├── field-notes/       the research JOURNAL — append-only notes (000-…) on *what changed our understanding* (explicitly NOT docs/specs/decisions); FIELD-NOTES.md = generated index (tools/build-field-notes.js), README.md = the charter
└── archive/           superseded / done notes, kept for history
```

## Which file does what

| Genre | File(s) | Changes… | Never holds |
|---|---|---|---|
| **Rules + pointers** | `../CLAUDE.md` | when a rule or tool changes | reference prose or runbooks (→ docs/ + file headers) |
| **Why/what** | `VISION.md` | rarely (direction shifts) | a roadmap or status (→ STATUS) |
| **State** | `STATUS.md` | constantly | design rationale (→ design/ or decisions/) |
| **Work list** | `POLISH_TODO.md` | as carts get polished | engine/API status (→ STATUS) |
| **Decisions** | `decisions/NNNN-*.md` | append-only (supersede, don't edit) | open questions (those are exploration) |
| **Exploration** | `design/*.md` | freely; it's a scratchpad | whether a thing shipped (→ STATUS) |
| **How-to** | `guides/*.md` | when the workflow changes | — |
| **Narrative** | `HANDOFF.md` | each work session | the roadmap (→ STATUS) |
| **Research journal** | `field-notes/*.md` | append-only (write a *new* note; don't rewrite) | docs / specs / decisions (those graduate to `design/` + `decisions/`) |

The **engine API reference itself** is not in `docs/` — it lives in the code:
`runtime/studio.h` (declarations) and `editor/src/studioDocs.js` (the bilingual one-liners
that drive autocomplete/hover/help). Don't maintain a second copy of the function list in
prose — link to those. (This is why the old "~100 functions" counts kept going stale.)

## When you change something

This mirrors **`CLAUDE.md` → "Making new things discoverable"**: each new thing goes to its
*home* and gets a one-line pointer from the index — `CLAUDE.md` itself stays rules + pointers,
never reference prose.

- New **CLI tool** (`tools/x.js`)? → full contract in the file's **header comment**, plus ONE line
  in `CLAUDE.md`'s tools list. Tools are **not** documented in `docs/` — the header is the source
  of truth.
- New **design / idea**? → a `design/*.md` file, linked from this README's layout tree (a one-line
  blurb). Don't append it to a grown doc; a one-line pointer here is the index.
- Shipped / cut / deferred a feature? → update **`STATUS.md`**, then the relevant design note.
- A choice is now *settled* (especially a "no")? → write a **`decisions/`** ADR-lite and link to it.
- Proposing new API? → **`design/api-notes.md`** (sound → **`design/audio-notes.md`**).
- New **audio idea**? → the **family index at the top of `design/audio-notes.md`** routes it
  (engines, stations, styles, racks, retrofits — one table). Rule of thumb: an audio
  investigation longer than ~a screen gets its own `design/` file + a one-line pointer,
  never a new § appended to the hub.
- Product direction or a core principle changed? → **`VISION.md`**.
- Splitting a section out of a grown doc? **If other files reference its §-numbers widely,
  keep the numbers verbatim in the new doc + leave a stub at the old spot**
  ([`instrument-engines.md`](design/instrument-engines.md) is the worked example); otherwise just renumber. The kept
  numbering exists *only* to serve live references — once those age out, renumbering the
  doc is ordinary cleanup, not a rule violation.
- Moved / split / renamed any doc? → **`node tools/lint-docs.js`** — verifies every
  relative `.md` link and doc-qualified §-reference (stub/parent resolutions show as
  soft notes; bare §-refs are deliberately unchecked). It caught 7 broken links from a
  2026-06 file move on its first run; trust it over grep.
- A doc went stale or started duplicating another? → prune it. This README is the contract.
