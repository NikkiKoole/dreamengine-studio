// acidwire — a big FIXED window; the cart shrinks the CANVAS to each device
// shape via de_resize(), so the engine letterboxes the real logical size.
// 940x700 holds every shape in device-matrix.md §2 (max side 688) with margin.
module.exports = { screenW: 940, screenH: 700, scale: 1 }
