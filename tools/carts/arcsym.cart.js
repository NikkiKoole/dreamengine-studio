// ARCSYM — a magnified cell grid plus HUD. No sprites or map; it draws everything
// with primitives. Just needs a roomier-than-default canvas so the 42×28 grid and
// the readout lines fit, at a chunky 2× window.
module.exports = {
  screenW: 372,
  screenH: 338,
  scale: 2,
};
