// Ear Trainer defaults to PORTRAIT (phone shape) — the layout is responsive, so
// it also works at 320x200 landscape, but the demand that spawned it is mobile.
module.exports = { screenW: 100, screenH: 160, scale: 4 };
