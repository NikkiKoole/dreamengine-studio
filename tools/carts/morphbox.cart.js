// MORPHBOX — no sprites/map; the whole box is primitives. The acidcandy device-face
// size (160x100), the shelf's tiny-toy standard. Built resizable (de:meta) so the real
// device is the responsive target; true device-fill reflow is the next step.
module.exports = {
  screenW: 160,
  screenH: 100,
  scale: 6,
};
