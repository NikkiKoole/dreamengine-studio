// RESPOND — a cart-land prototype of responsive layout. No sprites/map: the
// whole demo is primitives. A roomier landscape canvas so the draggable
// "virtual screen" has room to grow/shrink without hitting the bezel.
module.exports = {
  screenW: 400,
  screenH: 260,
  scale: 3,
};
