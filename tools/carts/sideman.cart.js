// The Side Man draws its own face: a walnut cabinet, a contact disc and a
// panel. No sprites, so this only widens the canvas enough for the disc and
// its control panel to sit side by side, the way the real lid is laid out.
module.exports = { screenW: 400, screenH: 240, scale: 3 };
