// WORDLE — primitives only, no sprite sheet or tile map.
// Just a taller portrait screen so the 6-row grid AND the QWERTY
// keyboard both fit comfortably with room for the result banner.
module.exports = {
  screenW: 240,
  screenH: 300,
  scale: 3,
};
