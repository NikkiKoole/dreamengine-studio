# Ambiguous — discriminator 3

We had no gate yet for "does it click", only for "is it in tune" — a TEST gate, not audio.
Separately, the rack has no gate effect on its chain, which IS an audio claim.
