# Flagged — the true-positive cases

These are the shapes the 2026-07-30 audit actually found in the repo.

- `technique` · spring-reverb crash · **missing** (there is no reverb engine) · fake it with noise.
- The vowel filter is single-input, not a vocoder (that needs two inputs — a future effect
  once the sidechain path lands).
