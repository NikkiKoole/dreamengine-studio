# Real-world instrument statements — discriminator 6

- **Clav**: none (the preset zeroes it; a real clav has no reverb of its own).

And discriminator 2's plural form, which a singular-only ACK regex missed once:

- no chorus WIRED yet (both now exist — `chorus()`), just not routed here.
