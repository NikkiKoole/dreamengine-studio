// FIXTURE for `node tools/lint-capability-claims.js --selfcheck` — NOT the real studio.h.
// Only the PROOF declarations matter: a capability is in the roster iff its symbol is
// declared here. `shallow` is deliberately ABSENT so the roster-exclusion guard has a
// subject, and there is no `octaver` anywhere (a genuinely-unbuilt capability).
// wah/leslie/tape are here for the RECALL cases (backlog.md, ackbleed.md): `tape` is an
// AMBIGUOUS word, so it also exercises the qualifier path the slash compound broke.
void reverb(float size, float damping);
void chorus(float rate, float depth, float mix);
void vocoder(float mix);
void gate(float threshold, int attack_ms, int release_ms);
void sample_record(int slot, int ms);
void wah(float sensitivity, float resonance, float mix);
void leslie(int speed, float drive, float balance, float doppler, float mix);
void tape(float wow, float flutter, float saturation);
void sidechain(int victim_bus, int key, float amount, int attack_ms, int release_ms);
