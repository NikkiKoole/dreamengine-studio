# Delta doc (fixture)

Named ONLY from inside a fenced code block elsewhere. If fence tracking breaks, that mention
escapes and this doc shows up as an unlinked mention — which is what makes the fence guard real.
