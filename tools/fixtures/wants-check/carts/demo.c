/* de:meta
{ "slug": "demo",
  "todo": ["mentions leslie() and shimmer in PROSE — must NOT count as wired"] }
de:meta */
// A comment naming leslie() must not count either — that bug shipped once.
void draw(void) {
    chorus(1.5f, 0.4f, 0.5f);      // wired
    multiband(0.7f, 0.6f, 0.8f, 0.5f, 1.0f);   // compression, via an ALTERNATE symbol
}
